const name ="Edu";
const age = 60;
const country = "Naija";

function add(x, y) {
    return x + y;
}

function subtract(x, y) {
    return x - y;
}

module.exports = { name, age, country, add, subtract };